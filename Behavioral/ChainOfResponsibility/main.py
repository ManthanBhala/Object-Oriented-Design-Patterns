from Client import *

logObj = Logger()
logObj.logMessage(LogProcessor.ERROR, "Execution")
logObj.logMessage(LogProcessor.DEBUG, "Need to debug this")
logObj.logMessage(LogProcessor.INFO, "Suggestions and info")